<section>
    <div class="login-register-area pt-85 pb-90 ">
        <div class="container">
            <div class="row">
                <div class="col-lg-7 col-md-12 ms-auto me-auto">
                    <div class="login-register-wrapper">
                        <div class="login-register-tab-list nav">
                            <a class="active" data-bs-toggle="tab" href="/login">
                                <h4> Enter OTP</h4>
                            </a>

                        </div>
                        <div class="tab-content">
                            <div id="lg1" class="tab-pane active">
                                <div class="login-form-container">
                                    <div class="login-register-form">
                                        <% if(locals.error) { %>
                                            <div class="alert alert-danger" role="alert">
                                                <%= error %>
                                            </div>
                                            <% } %>
                                                <form action="/verifyOTP" method="post">
                                                    <input type="tel" name="otp" placeholder="enter otp">
                                                    
                                                    
                                                    <div class="button-box">
                                                        <div class="login-toggle-btn">
                                                        </div>
                                                        <button type="submit">verify</button>
                                                        <!-- <button type="button" id="resend-otp-button" disabled onclick="resendOTP()">Resend OTP</button> -->
                                                    </div>
                                              </form>
                                              <!-- <div class="ml-5" id="timer-display"></div> -->
                                    </div>
                                </div>

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
</section>
<!-- <script>

    var timer = 60000;
    var interval = setInterval(function() {
      document.getElementById('timer-display').innerHTML = 'Resend OTP in ' + (timer / 1000) + ' seconds';
      timer -= 1000;
  
    
      if (timer <= 0) {
        clearInterval(interval);
        document.getElementById('resend-otp-button').disabled = false;
        document.getElementById('timer-display').innerHTML = 'Resend OTP';
      }
    }, 1000);

    function resendOTP() {
  fetch('/numberverification', { method: 'POST' })
    .then(response => {
      console.log('OTP sent successfully!');
    })
    .catch(error => {
    
      console.error(error);
    });
}
  </script> -->